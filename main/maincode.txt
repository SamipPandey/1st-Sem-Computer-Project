
int main() {
FILE *fp=fopen(MAIN_FILE.dat,wb);
fclose(fp);

     int role;

    role = login();

    if(role == 1) {
        adminMenu();
    }
    else if(role == 2) {
        userMenu();
    }
else if(role == 3) {
       doctormenu();
    }
    else {
        printf("Exiting system.\n");
    }
    
    return 0;
}







