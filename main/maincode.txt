#include<stdio.h>
#include<conio.h>
#include<string.h>
#include<maths.h>
#include<stdlib.h>
#define FILE_NAME "patients.dat"
#define MAX_NAME 30
#define MAX_DISEASE 40
#define ROOM_CHARGE 1500
#define DOCTOR_FEE 800
#define MEDICINE_COST 1200
#define ADMIN_USER "admin"
#define ADMIN_PASS "admin123"
#define DOC_USER "doctor"
#define DOC_PASS "doc123"
#define FILE_NAME "medical.dat"
void addPatient();
void viewPatients();
void searchPatient();
void updatePatient();
void deletePatient();
void addTreatment();
void viewTreatments();
void userMenu();
void adminMenu();
int login();
int doctormenu();
float calculateBill(int days);
int game();

enum Status {
    ADMITTED,
    DISCHARGED
};
struct Admin {
    char username[20];
    char password[20];
};
struct Patient {
    int id;
    char name[30];
    int age;
    char disease[40];
    float bill;
     struct Date admitDate;
    enum Status status;
};
struct Treatment {
    int patientID;
    char name[30];
    char disease[50];
    char medication[50];
    char precaution[100];
};
int main() {
FILE *fp=fopen(MAIN_FILE.dat,wb);
fclose(fp);

     int role;

    role = login();

    if(role == 1) {
        adminMenu();
    }
    else if(role == 2) {
        userMenu();
    }
else if(role == 3) {
       doctormenu();
    }
    else {
        printf("Exiting system.\n");
    }
    
    return 0;
}






